# Assets Directory (`src/assets/`)

This directory contains static uncompiled assets such as images, SVGs, icons, logos, and custom fonts.

## Guidelines:

- Place static media files here and reference them using relative paths or Vite aliases (`@/assets/logo.png`).
- Optimize image sizes before committing to Git.
